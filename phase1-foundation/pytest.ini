[pytest]
testpaths = tests
pythonpath = .
addopts =
    -ra
    --strict-markers
    --html=reports/phase1-report.html
    --self-contained-html
    --cov=src
    --cov-report=term-missing
    --cov-report=html:reports/coverage
markers =
    ui: marks Playwright browser UI tests
