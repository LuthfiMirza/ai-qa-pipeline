target_urls:
  - https://practicetestautomation.com/practice-test-login/
thresholds:
  ssim: 0.95
  anomaly_contamination: 0.05
  max_error_rate: 0.10
paths:
  baseline_dir: ../phase3-ai/b-visual-regression/baselines
  log_dir: logs
  report_dir: reports
steps:
  run_test_generation: true
  run_unit_tests: true
  run_visual: true
  run_anomaly: true
sample_requirement: "User dapat login dengan email dan password"
